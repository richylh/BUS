services:
  - type: web
    name: flask-app
    env: python
    plan: free
    buildCommand: "pip install -r app/requirements.txt"
    startCommand: "python run.py"
    envVars:
      - key: FLASK_ENV
        value: production
